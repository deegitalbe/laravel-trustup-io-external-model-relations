services:
  cli:
    build:
      context: .
      target: cli
      dockerfile: Dockerfile
    image: "laravel-trustup-io-external-model-relations-php-local"
    volumes:
      - ./:/opt/apps/app
  bun:
    build:
      context: .
      target: bun
      dockerfile: Dockerfile
    image: "laravel-trustup-io-external-model-relations-bun-local"
    volumes:
      - ./:/opt/apps/app
